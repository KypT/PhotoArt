<div class = 'container'>
  <div class = 'row'>
    <% @albums.each do | album | %>
      <div class = 'col-md-2'>
        <%= link_to "/gallery/#{album.id}" do %>
            <%= render partial: 'albumCard', locals: {album: album} %>
        <% end %>
      </div>
    <% end %>
  </div>
</div>