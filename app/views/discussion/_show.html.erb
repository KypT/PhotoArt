<div class = 'discussion-container'>
  <a href="" data-remote = true class = 'link load-more'>Предыдущие комментарии</a>
  <div class = 'discussion-messages'> </div>

  <% if user_signed_in?  %>
      <%= render partial: 'discussion/messageForm' %>
  <% else %>
      <div class = 'link auth'>
        <%= link_to 'Зарегистрируйтесь', new_user_registration_path, class: 'btn btn-primary'%> или
        <%= link_to 'Войдите', new_user_session_path, class: 'btn btn-success'%>
        для того, чтобы оставить комментарий
      </div>
  <% end %>
</div>
